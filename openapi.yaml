openapi: 3.1.0
info:
  title: India Public Holidays
  description: Gazetted holidays, restricted holidays, and bank holidays for current and future years
  version: 1.0.0
  license:
    name: public-domain
  contact:
    name: Oriz Free Public APIs
    url: https://oriz.in/apis/india-holidays
servers:
  - url: https://chirag127.github.io/india-holidays-api
    description: GitHub Pages
paths:
  /api/v1/data.json:
    get:
      summary: India Public Holidays
      description: Gazetted holidays, restricted holidays, and bank holidays for current and future years
      operationId: listData
      responses:
        '200':
          description: JSON array
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties: {
                            "id": {
                                      "type": "string"
                            },
                            "name": {
                                      "type": "string"
                            },
                            "country": {
                                      "type": "string"
                            },
                            "status": {
                                      "type": "string"
                            }
                  }
  /api/v1/individual/{id}.json:
    get:
      summary: Get single record
      operationId: getRecord
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Record object
          content:
            application/json:
              schema:
                type: object
                properties: {
                            "id": {
                                      "type": "string"
                            },
                            "name": {
                                      "type": "string"
                            },
                            "country": {
                                      "type": "string"
                            },
                            "status": {
                                      "type": "string"
                            }
                  }
        '404': { description: Not found }
  /api/v1/search-index.json:
    get:
      summary: Search index
      operationId: getSearchIndex
      responses:
        '200': { description: Search index }
  /api/v1/meta.json:
    get:
      summary: Dataset metadata
      operationId: getMeta
      responses:
        '200':
          description: Metadata
          content:
            application/json:
              schema: { type: object }
